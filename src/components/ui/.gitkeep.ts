/**
 * UI components directory — place primitive, reusable UI components here.
 *
 * Examples: Button, Input, Modal, Select, Badge, Card, Avatar
 *
 * Guidelines:
 * - Keep components generic and domain-agnostic
 * - Accept className prop for Tailwind overrides using cn() utility
 * - Use forwardRef for input-like components
 * - Export from index.ts barrel file
 */
