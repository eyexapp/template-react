/** Example entity — replace with your own domain models */
export interface User {
  id: string
  email: string
  name: string
  avatar?: string
  createdAt: string
  updatedAt: string
}

/** Example entity */
export interface ExampleItem {
  id: string
  title: string
  description: string
  completed: boolean
  createdAt: string
}
