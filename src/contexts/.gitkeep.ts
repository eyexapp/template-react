/**
 * Contexts directory — place React context providers here.
 *
 * For most state management needs, prefer Zustand stores (src/stores/).
 * Use React Context for:
 * - Dependency injection (e.g. service instances)
 * - Theme / locale context that wraps the entire tree
 * - Data that changes infrequently and is read by many components
 */
